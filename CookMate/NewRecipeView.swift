//
//  NewRecipeView.swift
//  CookMate
//
//  Created by Sabyasachi Sahu on 27/02/24.
//- Finalise the steps that needs to be done
// - Then design the simple form
// - on saving the form should be save in the form of a card (with estimated time to cook)
// - Whenver someone clicks on the card, the user is taken to a screen where the user  can start the timer for different steps

import SwiftUI

struct NewRecipeView: View {
    var body: some View {
        Text(/*@START_MENU_TOKEN@*/"Hello, World!"/*@END_MENU_TOKEN@*/)
    }
}

struct NewRecipeView_Previews: PreviewProvider {
    static var previews: some View {
        NewRecipeView()
    }
}
